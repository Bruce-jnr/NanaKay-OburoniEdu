{
  "name": "nanakay",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"node --watch server.js\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "postmark": "^5.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
