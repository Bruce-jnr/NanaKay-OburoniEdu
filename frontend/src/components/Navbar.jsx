import {useEffect,useState} from 'react';
export default function Navbar(){const[open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>20);f();addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);const close=()=>setOpen(false);return <nav className={`navbar ${scrolled?'scrolled':''} ${open?'open':''}`}><div className="container nav-inner"><a className="brand" href="#home" onClick={close}>Oburoni Educational Consult<span>Global education experts</span></a><button className="menu-btn" aria-label="Toggle navigation" aria-expanded={open} onClick={()=>setOpen(!open)}>{open?'×':'☰'}</button><div className="nav-links"><a href="#home" onClick={close}>Home</a><a href="#about" onClick={close}>About Us</a><a href="#services" onClick={close}>Services</a><a href="#testimonials" onClick={close}>Stories</a><a href="#contact" onClick={close}>Contact</a><a className="btn btn-primary" href="#contact" onClick={close}>Apply Now</a></div></div></nav>}
